# URL of the deployed RNAPeaks R/Plumber backend (HF Space)
# Never use NEXT_PUBLIC_ — this must stay server-side only
HF_SPACE_URL=https://your-space.hf.space

# Shared secret token — must match the token configured on the R/Plumber backend
HF_SECRET_TOKEN=your-strong-random-secret
